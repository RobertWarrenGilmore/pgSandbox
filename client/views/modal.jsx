'use strict'
import React from 'react'

const Modal = (props) => (
  <div className='modalWrapper'>
    <div className='modal'>
      {this.props.children}
    </div>
  </div>
)

export default Modal
